В терминале перейдите в папку с кодом и выполните последовательно команды:<br>
python -m venv venv<br>
./venv/Scripts/Activate<br>
python -m pip install --upgrade pip<br>
pip install -r requirements.txt<br>
python main.py
