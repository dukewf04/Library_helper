В терминале перейдите в папку с кодом и выполните последовательно команды:
python -m venv venv
./venv/Scripts/Activate
python -m pip install --upgrade pip
pip install -r requirements.txt
python main.py
